# Backend deps. Install via ../scripts/setup-venv.sh (uses uv — see that file
# for why not Homebrew python/pip on macOS 26).

# Stage 0–1
websockets>=12
mlx-lm>=0.31

# Later stages (Apple Silicon only):
#   Stage 2  mlx-whisper        # STT
#            mlx-audio          # TTS (Kokoro)
#            onnxruntime        # Silero VAD (never the silero-vad pkg; D10)
#   Stage 4  SQLite FTS5 is in the Python stdlib
