# Backend deps. Install via ../scripts/setup-venv.sh (uses uv — see that file
# for why not Homebrew python/pip on macOS 26).

# Stage 0–1
websockets>=12
mlx-lm>=0.31

# Stage 2 (Apple Silicon only)
mlx-whisper>=0.4      # STT (whisper-large-v3-turbo)
mlx-audio>=0.2        # TTS (Kokoro); note: pulls torch — shipping debt, see ROADMAP
misaki[en]            # Kokoro's G2P text processing (required)
onnxruntime>=1.20     # Silero VAD (never the silero-vad pkg; D10)
soundfile             # audio file I/O (tools/tests)
scipy                 # resampling in tools/tests

# Stage 4  SQLite FTS5 is in the Python stdlib
